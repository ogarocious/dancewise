<%= form_with(model: giraffe) do |form| %>
  <% if giraffe.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(giraffe.errors.count, "error") %> prohibited this giraffe from being saved:</h2>

      <ul>
        <% giraffe.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
